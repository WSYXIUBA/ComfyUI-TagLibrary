自 v1.7.1 起的主要变化（详见 CHANGELOG.md）：

## 新增

- **NSFW 扩展包**：词表以扩展包分发（`tag_library.ext.json` / `nsfw_*.json`，不入发布包，Registry 合规），新增 7 个 NSFW 槽位，词源经 danbooru post_count 校验
- **出词效率套件**：场景条三开关（单人锁 / 简洁背景 / 人物特写）、NSFW 强度三档、分轴重摇、批量探索 8~30 条、Prompt 吸收器
- **预设系统**：出厂 13 个 + 我的预设；钉选词 / 排除域 / 随机配置存为命名预设，下拉一键载入，预设是约束不是锁死
- **标签就地编辑**：详情页流水线首页、面板改用标准控件、编辑层字段完整往返
- **库文件即数据**：删掉 .md 镜像层，整库只有 .json，导出/导入直接进出原文件
- **手动/半手动重构**：三栏挑选器 + 规则分层 + 约束可见

## 修复

- 单人锁漏人 / 简背景漏词 / 人数锚点缺失 / 未成年锁顺序漏 / NL 破句
- 热同步移出读路径（修「连续输出越跑越慢」）
- 用户库自动滚动备份（防再次无声丢库）
- 人数词库对齐 danbooru：12 个模型读不懂的自造词换成有贴数的真标签
  （`0others`→`solo`、`trio`→`3girls`、`pair`→`2girls`、`group of girls`→`multiple girls`、`1girl and 1boy`→`couple` 等）

## 数据

13 条轴 / 66 槽位 / 4458 词 / 21 份武器·物品档案 / 57 组互斥域 / 37 族 NL 句式。
升级不丢用户库；门禁 15 项全过。
