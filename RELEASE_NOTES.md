# ComfyUI-TagLibrary v1.12.2

结构化标签库节点：双模式随机引擎 + 反冲突系统 + 管理页，输出 STRING 直连 CLIPTextEncode。

## 亮点

- **库文件即数据**：整库只有 `tag_library.json`（出厂）+ `tag_library.user.json`（我的）+ `data/default/taglib/*.json`（规则）。
  管理页「📤 导出整库 / 📤 导出我的 / 📥 导入 .json」直接进出原文件，文件里带 `_说明` 写清格式与规则。
  1.12.0 删掉了 `.md` 镜像层与文件夹双向同步（含单向删除开关）。
- **预设**：面板主区直接选 / 存 / 删。保存的是当前面板**全部词标签**，召唤时全部打钉 —— 自动模式不再覆盖它们；
  换预设会先清掉上一个预设带进来的词（不叠加）；带进来的词按当前显示语言补齐中文。
- **面板**：预设等控件并入自适应流式区（不再各占一整行）；节点原生的 模式 / 随机种子 / 生成后种子动作
  三个 widget 已隐藏（功能都在面板里）；清空标签、显示语言等常用项从 ⋯ 菜单搬回面板，清空可撤销。
- **版本自检**：插件更新后面板顶部提示「插件已更新 → 点这里刷新」，不用猜要不要刷新页面。
- **NSFW 体系**：三档强度（标准 / 强调 / 纯欲）、扩展包（`ext.*`，不入发布包）、互斥域、未成年锁。
- **质量门禁**：离线 15 项 + 在线 6 项 —— 含 708 条重度提示词矩阵、3000 次压力、真机 60 次生成、
  4 个浏览器 UI 巡检（面板 / 主题 / 弹层 / 全功能端到端）。

## 升级注意

- 从 1.11 及更早版本升级：`data/default/taglib/` 下的分类文件夹与 `.md` 标签文件不再使用，可自行删除；
  标签数据一直都在 `tag_library.json` / `tag_library.user.json` 里，不会丢。
