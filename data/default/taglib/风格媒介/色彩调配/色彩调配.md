# 风格媒介
## 色彩调配

pastel colors(粉彩色调), muted colors(低饱和), vivid colors(鲜艳色彩), contrast colors(对比色), dark palette(暗色调), warm colors(暖色调)
cool colors(冷色调), monotone(单色调), complementary colors(互补色), analogous colors(类似色), triadic colors(三色系), limited palette(有限色板)
duotone(双色), neon palette(霓虹配色), earth tones(大地色系), creamy colors(奶油色系), desaturated(低饱和), oversaturated(高饱和)
iridescent palette(幻彩配色)
