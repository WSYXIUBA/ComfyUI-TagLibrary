# 构图镜头
## 构图

centered composition(居中构图), symmetrical composition(对称构图), dynamic angle composition(动感构图), negative space(留白构图), diagonal composition(对角线构图), framing composition(框式构图)
leading lines(引导线构图), triangular composition(三角构图), golden ratio(黄金比例构图), depth of field background(景深虚化背景), blurry foreground(前景虚化), layered depth(层次景深)
vignetting(暗角), frame within frame(画中画构图), rule of thirds(三分法), golden ratio composition(黄金比例构图), radial composition(放射构图), spiral composition(螺旋构图)
frame within a frame(框中框构图), off-center composition(偏心构图), low angle composition(仰角构图), fill the frame(满幅构图), juxtaposition(并列对比构图)
