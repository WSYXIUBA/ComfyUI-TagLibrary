[project]
name = "comfyui-taglibrary"
description = "结构化标签库节点: 双模式随机引擎 + 反冲突系统 + 文件夹式存储热同步 + 管理页, 输出 STRING 直连 CLIPTextEncode。"
version = "1.2.1"
license = { text = "MIT" }
dependencies = []

[project.urls]
Repository = "https://github.com/WSYXIUBA/ComfyUI-TagLibrary"

[tool.comfy]
PublisherId = "wsyxiuba"
DisplayName = "🏷 Tag Library 标签库"
Icon = ""
Tags = ["prompt", "tagging", "utility", "random"]
